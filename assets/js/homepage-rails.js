/**
 * Homepage Rails - Minimal initialization
 * CSS handles scrolling via overflow-x: auto
 */
(function() {
    'use strict';

    // No JS needed - CSS handles horizontal scroll natively
    // This file kept for potential future enhancements (e.g., snap indicators)

})();
